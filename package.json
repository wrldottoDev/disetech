{
  "name": "disetech-page",
  "version": "0.1.0",
  "private": true,
  "description": "DISETECH landing. Static HTML + ES modules, no build step.",
  "scripts": {
    "dev": "node server.js",
    "start": "node --env-file=.env server.js",
    "check": "node --check server.js && node --check src/main.js",
    "test": "node --test \"server/*.test.js\"",
    "smtp-sink": "node server/smtp-sink.js"
  },
  "type": "module",
  "dependencies": {
    "nodemailer": "^10.0.2"
  }
}
